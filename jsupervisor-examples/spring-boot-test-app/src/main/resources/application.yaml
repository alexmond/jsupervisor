spring:
  application:
    name: spring-boot-sample

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  file:
    name: srv.log

---
spring:
  config:
    activate:
      on-profile: srv1
server:
  port: 8081

logging:
  file:
    name: srv1.log

---
spring:
  config:
    activate:
      on-profile: srv2
server:
  port: 8082
